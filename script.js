// Load up this cmd prompt curl command to a text file. <Done!>
// If this is in the viewport... <Make intersection observer>
// Print each frame one by one. <Somehow find a way to print 'frames'>
// Repeat it. <Make this a loop>
// Stop it when it exits the viewport. <Remove the observer class to stop it>

let sdf = document.getElementById("rick");
console.log(sdf);
